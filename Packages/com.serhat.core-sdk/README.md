# Serhat Core SDK

Preview package containing shared runtime primitives used by the embedded Serhat Forge packages.

The package is embedded in the template under `Packages/com.serhat.core-sdk`. Keep it installed while another `com.serhat.*` package references its APIs.

See the repository root `README.md` and `TEMPLATE_README.md` for project-level setup.
